#include <bits/stdc++.h>
using namespace std;

int main() {
    string s, tmp; 
    stringstream ss(s);
    vector<string> words;

    while(getline(ss, tmp, ' ')){
        words.push_back(tmp);

    }
}